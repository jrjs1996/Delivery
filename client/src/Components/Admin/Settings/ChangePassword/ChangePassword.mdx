
---
name: ChangePassword
menu: Components
route: /Admin/Settings/ChangePassword/ChangePassword.mdx
---
import { useState } from 'react';
import { Playground, Props } from 'docz';
import { ChangePasswordComponent } from './ChangePassword';
import { adminsMock } from '../../../../tests/mocks';

# Change Password

Updated 09/07/2019

Form for the current user to change their password.

<Props of={ChangePasswordComponent} />

<Playground>
  {() => {
    const [message, setMessage] = useState('');
    
    return(
      <div>
        <p>{message}</p>
        <ChangePasswordComponent
          changeCurrentAdminPassword={(id, password) => {
            setMessage(`id: ${id} password: ${password}`)
          }}
          currentAdmin={adminsMock[0]}
        />
      </div>
    );
  }}
</Playground>