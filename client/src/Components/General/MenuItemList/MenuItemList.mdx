
---
name: MenuItemList
menu: Components
route: /components/Admin/Menu/MenuItemList/MenuItemList.mdx
---
import { useState } from 'react';
import { menuItemsMock } from '../../../../tests/mocks'
import { Playground, Props } from 'docz';
import MenuItemList from './MenuItemList';

# Menu Item List

Updated 20/06/2019

Displays a list of menu items which the user can interact with.

<Props of={MenuItemList} />

<Playground>
  {() => {
    const [menu, setMenu] = useState(menuItemsMock)

    const [message, setMessage] = useState("")

    return(
      <div>
        <p>{message}</p>
        <MenuItemList
          menu={menu}
          onSelect={i => {setMessage(`You selected: ${i.title}`)}}
          onDelete={n => {setMessage(`*Delete ${n} API call*`)}}
        />
      </div>
    )
  }}
</Playground>